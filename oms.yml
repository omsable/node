oms: 1

info:
  version: 1.0.0
  title: Nodejs
  description: Execute a Node file.
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

actions:
  execute:
    help: Execute a javascript file.
    format:
      command: ['node']
    arguments:
      path:
        type: string
        required: true
        help: Path to the JavaScript file to execute.

volumes:
  js:
    target: /data
