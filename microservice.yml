commands:
  entrypoint:
    help: Execute a javascript file.
    arguments:
      path:
        type: string
        required: true
        help: Path to the JavaScript file to execute.

volumes:
  js:
    target: /data
